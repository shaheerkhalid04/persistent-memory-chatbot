# --- UI ----------------------------------------------------------------------
streamlit>=1.40,<2.0

# --- Config ------------------------------------------------------------------
python-dotenv>=1.0

# --- LLM backend -------------------------------------------------------------
# Groq is the default: free tier, no card, fastest inference.
groq>=0.11

# Alternatives — uncomment the one you configure a key for. Imports are lazy,
# so an uninstalled provider costs nothing until it is selected.
# google-genai>=1.0        # Gemini
# openai>=1.50             # OpenAI

# --- Memory backend ----------------------------------------------------------
# Default is SQLite on disk (stdlib, nothing to install).
# Uncomment for the hosted Mem0 platform via MEMORY_BACKEND=mem0.
# mem0ai>=0.1.50

# --- Tests -------------------------------------------------------------------
# pytest>=8.0
